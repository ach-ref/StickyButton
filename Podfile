# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'StickyButton' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for StickyButton

  target 'StickyButtonTests' do
    # Pods for testing
  end

end

target 'StickyButtonExample' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'StickyButton', :path => '.', :target => 'StickyButtonExample'

end
